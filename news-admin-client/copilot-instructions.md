# Copilot Instructions for news-admin-client

- This is a standalone administrative client for the news multi-agent system.
- It should connect to all MCP servers/agents, trigger workflows, monitor metrics, and provide admin controls.
- The client may use a CLI (with prompt_toolkit/rich) or a web dashboard (Flask/FastAPI) for the UI.
- Update this file as the client logic is implemented.

# Copilot Instructions for mcp-prometheus-agent

- This agent runs a Prometheus server to monitor all MCP agents and the admin client.
- It should be started as a standalone process using the provided `prometheus.yml` config.
- The admin client can query Prometheus for metrics and health data.
- Update this file as integration with the admin client is implemented.
